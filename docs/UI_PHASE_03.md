# Step 3 — Approved Opening visual restoration (local candidate)

Scope: the approved `ORBIS-OPENING-LOGIN-PREVIEW.html` is the **Welcome visual** reference. The A-to-Z preview is a later accounting-flow reference, not a replacement Welcome. This patch is pending owner screenshot approval and does not authorize a deployment.

- Restored SVG orbit mark, three-line welcome title, original gradient/card hierarchy, floating badges, safe-area and short-height responsive rules. Demo/sample financial values remain explicitly illustrative; 12% is a sample growth visual only.
- Preserved Step 2 Bengali/English/Hindi selector, preference keys, Khata Boi → Farming → Lottery module lineup, demo-only login with disabled credential fields and all non-sensitive session behavior.
- Improved login input icon/spacing parity and original workspace greeting/avatar/notice layout without inventing real Auth or organizations.
- No signup route or actual accounting engine is introduced; these are future approved implementation decisions, not an installer error.
- Source test assertions now include orbit SVG and three-line Bengali heading; actual Android screenshot/back/reload and final certification remain owner-side evidence.
- Installer must verify local HEAD `fa19d1a16a5977117b8b490baced357caaad85e2` and exact existing source SHA-256, back up all targets into Downloads, run targeted verification, and never commit/push/deploy.
